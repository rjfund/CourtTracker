<%= form_for(kase) do |f| %>

  <div class="field">
    <%= f.label :uid, class: 'label' %>
    <div class="control">
      <%= f.text_field :uid, class: 'input' %>
    </div>
    <p class="help">Unique ID for case (e.g. RT158392)</p>
  </div>

<% end %>
